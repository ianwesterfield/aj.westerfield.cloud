# Orchestrator Service Dependencies
# NOTE: Includes executor dependencies (merged service)

fastapi>=0.104.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
aiohttp>=3.9.0  # For async HTTP (gossip peer discovery)
requests>=2.31.0  # For synchronous HTTP calls (intent extraction)
pydantic>=2.5.0
python-dotenv>=1.0.0

# Executor dependencies (merged)
pathspec>=0.11.0  # For gitignore pattern matching in file_handler

# Training data capture
qdrant-client>=1.7.0
sentence-transformers>=2.2.0

# gRPC for FunnelCloud agent communication
grpcio>=1.60.0
grpcio-tools>=1.60.0
