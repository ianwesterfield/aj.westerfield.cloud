# =============================================================================
# AJ Docker Configuration
# =============================================================================

# Docker Hub username for base images (default: AJ for local builds)
BASE_REGISTRY=ianwesterfield

# =============================================================================
# Workspace Configuration
# =============================================================================

# Host path to mount as /workspace in orchestrator/executor containers
# This gives AJ access to your local code directories
# TODO: Udpate to use sessional workspace paths
HOST_WORKSPACE_PATH=/mnt/c/Code/aj.westerfield.cloud/

# =============================================================================
# Resource Limits
# =============================================================================

# Max parallel tasks for orchestrator
MAX_PARALLEL_TASKS=20

# =============================================================================
# FunnelCloud Agent Discovery
# =============================================================================

# Local agent host for discovery proxy (the Windows host running FunnelCloud Agent)
# For WSL2: This is the gateway IP (run: cat /etc/resolv.conf | grep nameserver)
# For native Linux: Use the IP of the machine running the local agent
# Note: All other agents are discovered dynamically via gossip (no hardcoding needed)
FUNNEL_LOCAL_AGENT_HOST=172.25.224.1
# Gossip seed host - ONE reachable agent to bootstrap cross-subnet discovery
# (only needed when the local agent's subnet can't multicast to other networks)
# Example: DOMAIN01 on the 192.168.10.x network
FUNNEL_GOSSIP_SEED_HOST=192.168.10.166

# =============================================================================
# Hugging Face Configuration
# =============================================================================

# HuggingFace API token for model downloads and inference API
# Token is loaded from secrets/huggingface_pat.txt