# QLoRA Fine-tuning Configuration for DeepSeek-R1-Distill-Qwen-32B
# Optimized for agent workloads: reasoning, planning, code generation

model:
  base_model: "deepseek-ai/DeepSeek-R1-Distill-Qwen-32B"
  quantization: "4bit"  # QLoRA uses 4-bit base
  max_seq_length: 4096
  dtype: "bfloat16"

lora:
  r: 64                    # LoRA rank - higher for complex tasks
  lora_alpha: 128          # Scaling factor
  lora_dropout: 0.05
  target_modules:
    - "q_proj"
    - "k_proj"
    - "v_proj"
    - "o_proj"
    - "gate_proj"
    - "up_proj"
    - "down_proj"
  bias: "none"
  task_type: "CAUSAL_LM"

training:
  output_dir: "./checkpoints-r1"
  num_train_epochs: 3
  per_device_train_batch_size: 2      # Bump to 2 for better GPU utilization
  gradient_accumulation_steps: 4      # Keep effective batch = 8
  learning_rate: 2.0e-4
  weight_decay: 0.01
  warmup_ratio: 0.03
  lr_scheduler_type: "cosine"
  logging_steps: 10
  save_steps: 100
  eval_steps: 100
  save_total_limit: 3
  fp16: false
  bf16: true
  gradient_checkpointing: true
  optim: "paged_adamw_8bit"
  max_grad_norm: 0.3
  group_by_length: true
  report_to: "none"

data:
  train_files:
    - "data/intent_classification.jsonl"
    - "data/tool_selection.jsonl"
    - "data/task_planning.jsonl"
    - "data/guardrails.jsonl"
    - "data/architecture_qa.jsonl"
  eval_split: 0.1
  shuffle: true
  seed: 42

# Dataset format - R1-Distill uses ChatML (same as Qwen)
prompt_template: |
  <|im_start|>system
  {system}<|im_end|>
  <|im_start|>user
  {user}<|im_end|>
  <|im_start|>assistant
  {assistant}<|im_end|>

export:
  output_dir: "./output-r1"
  merge_adapter: true
  quantize_gguf: true
  gguf_quantizations:
    - "Q4_K_M"
    - "Q5_K_M"
